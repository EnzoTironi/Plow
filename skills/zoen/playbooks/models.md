# Models

The talker, the opening, vision, fallback, and every `delegate_task`
child are `openai/gpt-5.6-luna`. Delegation reasons. The runtime pins
that. The spawn has no model field. Do not name another slug.

Leaves never sequence iMessage. Do not pass Opus, Kimi, or GLM.
